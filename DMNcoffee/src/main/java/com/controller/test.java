package com.controller;

import java.util.List;
import java.util.StringTokenizer;

import com.dto.CategoryDTO;
import com.dto.ProductDTO;
import com.service.CategoryService;
import com.service.ProductService;

public class test {

	public static void main(String[] args) {

		// product jdbc연습
		/*
		 * ProductService pservice = new ProductService(); List<ProductDTO> product =
		 * pservice.select(); //System.out.println(product.get(0).getPdnm());
		 * 
		 * //메뉴이름 토큰 분리 연습
		 *  String str = "콜드부루 브라운슈가 버블라떼"; StringTokenizer st = new
		 * StringTokenizer(str," ");
		 * 
		 * if(st.countTokens()==3) { System.out.println(st.nextToken());
		 * System.out.println(st.nextToken()); System.out.println(st.nextToken()); }else
		 * if(st.countTokens()==2) { System.out.println(st.nextToken());
		 * System.out.println(st.nextToken()); }else {
		 * System.out.println(st.nextToken()); }
		 * 
		 * 
		 * CategoryService cservice = new CategoryService(); List<CategoryDTO> ctnm =
		 * cservice.select(); System.out.println(ctnm.get(5).getCtnm());
		 */

	}// main

}
