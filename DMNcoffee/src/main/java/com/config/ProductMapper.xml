<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ProductDTO">

	<select id="select" resultType="com.dto.ProductDTO" parameterType="int">
	select pdno, pdnm, pdprice, ctno ,epdnm
	from product
	where ctno= #{ctno}
	</select>
	
	<select id="selectPDNO" resultType="com.dto.ProductDTO" parameterType="int">
	select pdno, pdnm, pdprice, ctno ,epdnm
	from product
	where pdno= #{pdno}
	</select>
	
</mapper>